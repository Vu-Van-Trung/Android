name: login_app
description: Flutter Login App with TOTP Authentication
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  shared_preferences: ^2.5.4
  pin_code_fields: ^8.0.1
  google_fonts: ^6.1.0
  image_picker: ^1.2.1
  http: ^1.6.0
  socket_io_client: ^3.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
